# ARKlinux substrate identities only.
# The private ark-services package provides /usr/lib/sysusers.d/ark.conf for the
# canonical A.R.K. runtime user/group when integrated.

g ark-io -
u ark-hardwared -:ark-io "ARKlinux hardware inventory observer" /var/empty /usr/bin/nologin
