# ARKlinux substrate identities only.
# The canonical private ark-services package creates the general `ark` runtime user/group.

g ark-io -
u ark-hardwared -:ark-io "ARKlinux hardware inventory observer" /var/empty /usr/bin/nologin

# Kyle, Aletheia, Joey, HRM, Kenny, the A.R.K. event/evidence runtime, and the
# trading domain are provided by the private A.R.K. packages. ARKlinux does not
# create heartbeat-only identities that could be mistaken for those roles.
